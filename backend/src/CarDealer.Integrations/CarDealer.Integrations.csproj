<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Integrations hosts external provider adapters (master prompt section 6).

    Phase 0 ships no external providers - the first one is the Carapis adapter in
    Phase 0.5. The project exists now so the boundary is established before anything
    needs to cross it.
  -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="8.0.*" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.*" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\CarDealer.Application\CarDealer.Application.csproj" />
    <ProjectReference Include="..\CarDealer.Domain\CarDealer.Domain.csproj" />
  </ItemGroup>

</Project>
